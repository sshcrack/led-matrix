# Laptop MCP sandbox entry

This file is generated by laptop-mcp for this disposable sandbox. Read it first, then follow the repository's own agent/build documentation. It belongs only to the sandbox baseline and is not written to the source checkout.

## Sandbox identity

- Session: `windows-record-fix`
- Profile: `default`
- Base ref: `HEAD`
- Source branch: `tech-debt-refactor`
- Base commit: `3afe00e455a2dd7d8f1bd8d6eb59c0ec84d89b60`
- Mirrored dirty source state: `true`
- Repository config: .laptop-mcp.toml (repository-local sandbox configuration)
- Configured copied inputs: none
- Submodules: recursive

## Important files

- `AGENTS.md` — repository agent instructions
- `README.md` — project overview and usage
- `.laptop-mcp.toml` — sandbox image/resource/workspace configuration
- `LAPTOP_MCP_CONFIG.md` — project-specific laptop-mcp notes

## Sandbox workflow

1. Work only inside `/workspace`; the real source checkout is not writable from this container.
2. Inspect the important files above and the relevant language/build manifests before changing code.
3. Keep changes in logical Git commits. Sandbox Git signing is intentionally disabled.
4. Run the repository's normal validation before handoff.
5. Do not commit this generated entry file in an agent commit; it is already part of the private sandbox baseline and is excluded from exported sandbox-only changes.

## Runtime notes

- `$LAPTOP_MCP_TMPDIR` is executable and is also used by `TMPDIR`, `TEMP`, and `TMP`; ordinary `/tmp` remains `noexec`.
- Shared package/compiler caches live under `/cache`; build outputs and virtual environments should remain sandbox-local.
- Repository-specific dependencies, copied inputs, environment, submodules, and resource limits come from `.laptop-mcp.toml` when present.
